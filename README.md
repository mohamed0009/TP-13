# README

This project is a Java Spring Boot application. Please refer to the source code and `pom.xml` for details about the structure and configuration.

## Structure
- `src/main/java/` - Java source code
- `src/main/resources/` - Application resources
- `src/test/java/` - Test sources
- `pom.xml` - Maven configuration

## How to Build and Run
1. Make sure you have Java and Maven installed.
2. Run `mvnw spring-boot:run` to start the application.

## Notes
- No comments were found in the code to change.
- For more information, see the code files in the `src` directory.
